# คู่มือการใช้งาน Water Drinking Reminder Bot 🚰

## 📋 สารบัญ

1. [ภาพรวมระบบ](#ภาพรวมระบบ)
2. [การติดตั้งครั้งแรก](#การติดตั้งครั้งแรก)
3. [การใช้งานระบบ](#การใช้งานระบบ)
4. [การจัดการ Service](#การจัดการ-service)
5. [การตรวจสอบ Log](#การตรวจสอบ-log)
6. [การแก้ไขปัญหา](#การแก้ไขปัญหา)
7. [การปรับแต่ง](#การปรับแต่ง)
8. [คำถามที่พบบ่อย (FAQ)](#คำถามที่พบบ่อย-faq)

---

## 🎯 ภาพรวมระบบ

Water Drinking Reminder Bot เป็นระบบแจ้งเตือนดื่มน้ำอัตโนมัติผ่าน Telegram ที่ทำงานบนเครื่อง Mac ของคุณ

### ✨ คุณสมบัติหลัก

- ⏰ **แจ้งเตือน 5 ครั้งต่อวัน** ตามเวลาที่กำหนด
- 💬 **ส่งข้อความผ่าน Telegram** รับได้ทุกที่ทุกเวลา
- 🔄 **รันอัตโนมัติ** เมื่อเปิดเครื่องและ login
- 🔁 **Auto-restart** หากระบบเกิดข้อผิดพลาด
- 📝 **บันทึก Log** สามารถตรวจสอบประวัติได้

### ⏰ เวลาแจ้งเตือน

| เวลา | ข้อความแจ้งเตือน |
|-------|---------------------|
| **09:00** | Good morning! Time to drink water! 💧 |
| **12:00** | Lunch time reminder! Stay hydrated! 🌊 |
| **15:00** | Afternoon hydration break! 💦 |
| **18:00** | Evening water reminder! 🚰 |
| **21:00** | Last call for water today! Good night! 🌙 |

---

## 🚀 การติดตั้งครั้งแรก

> **หมายเหตุ:** หากคุณได้ติดตั้งไปแล้ว สามารถข้ามส่วนนี้ได้

### ขั้นตอนที่ 1: ติดตั้ง Python Dependencies

```bash
cd /Users/coraline/Documents/alert_water
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### ขั้นตอนที่ 2: ตรวจสอบไฟล์ .env

ตรวจสอบว่าไฟล์ `.env` มีข้อมูลที่ถูกต้อง:

```bash
TELEGRAM_BOT_TOKEN=8522822747:AAFaEdHCnWHU0WHWWoClGBRXOdrZw8-WpTE
TELEGRAM_CHAT_ID=2058436657
```

### ขั้นตอนที่ 3: ทดสอบระบบ

```bash
source venv/bin/activate
python main.py
```

คุณควรจะได้รับข้อความทดสอบบน Telegram:
```
🧪 Test message - Water Reminder Bot is active!
```

กด `Ctrl+C` เพื่อหยุดการทดสอบ

### ขั้นตอนที่ 4: ตั้งค่าให้รันอัตโนมัติ (launchd)

ไฟล์ service ถูกสร้างไว้แล้วที่:
```
~/Library/LaunchAgents/com.waterreminder.daemon.plist
```

เปิดใช้งาน service:

```bash
# โหลด service (ทำครั้งเดียว)
launchctl load ~/Library/LaunchAgents/com.waterreminder.daemon.plist

# เริ่ม service
launchctl start com.waterreminder.daemon
```

ตรวจสอบว่า service ทำงาน:

```bash
launchctl list | grep waterreminder
```

ควรแสดงผลลัพธ์ประมาณนี้:
```
51567    0    com.waterreminder.daemon
```

---

## 📖 การใช้งานระบบ

### การใช้งานปกติ

หลังจากติดตั้งและเปิดใช้งาน service แล้ว ระบบจะทำงานอัตโนมัติ:

1. **เมื่อเปิดเครื่องและ login** → บอทเริ่มทำงานอัตโนมัติ
2. **ระหว่างวัน** → แจ้งเตือนตามเวลาที่กำหนด 5 ครั้ง
3. **หากบอท crash** → restart อัตโนมัติด้วย KeepAlive

### รับข้อความแจ้งเตือน

ข้อความจะถูกส่งไปยัง Telegram bot: `@drink_water_local_bot`

ตัวอย่างข้อความที่คุณจะได้รับ:

```
💧 Good morning! Time to drink water!

🌊 Lunch time reminder! Stay hydrated!

💦 Afternoon hydration break!
```

---

## 🛠️ การจัดการ Service

### ดูสถานะ Service

```bash
# ตรวจสอบว่า service กำลังทำงานหรือไม่
launchctl list | grep waterreminder
```

ผลลัพธ์:
- ถ้าเห็น PID ตัวเลข → service ทำงานอยู่ ✅
- ถ้าไม่เห็น → service ไม่ทำงาน ❌

### ดู Log แบบ Real-time

```bash
tail -f /Users/coraline/Documents/alert_water/logs/daemon.log
```

กด `Ctrl+C` เพื่อออกจากโหมดดู log

### หยุด Service ชั่วคราว

```bash
launchctl stop com.waterreminder.daemon
```

### เริ่ม Service ใหม่

```bash
launchctl start com.waterreminder.daemon
```

### Restart Service

```bash
launchctl restart com.waterreminder.daemon
```

หรือใช้คำสั่ง stop แล้ว start:

```bash
launchctl stop com.waterreminder.daemon
launchctl start com.waterreminder.daemon
```

### ปิดใช้งาน Service ถาวร

```bash
# หยุดและถอดการโหลด service
launchctl unload ~/Library/LaunchAgents/com.waterreminder.daemon.plist
```

### เปิดใช้งาน Service อีกครั้ง

```bash
launchctl load ~/Library/LaunchAgents/com.waterreminder.daemon.plist
```

---

## 📊 การตรวจสอบ Log

### ตำแหน่งไฟล์ Log

- **Main Log**: `/Users/coraline/Documents/alert_water/logs/daemon.log`
- **Error Log**: `/Users/coraline/Documents/alert_water/logs/daemon.error.log`

### ดู Log ล่าสุด

```bash
# ดู 20 บรรทัดล่าสุด
tail -n 20 /Users/coraline/Documents/alert_water/logs/daemon.log

# ดูแบบ real-time
tail -f /Users/coraline/Documents/alert_water/logs/daemon.log
```

### ดู Error Log

```bash
cat /Users/coraline/Documents/alert_water/logs/daemon.error.log
```

### ตัวอย่าง Log ที่ดี

```
2026-01-18 10:00:06,482 - __main__ - INFO - Loading configuration...
2026-01-18 10:00:06,483 - config - INFO - Configuration validated successfully
2026-01-18 10:00:06,483 - __main__ - INFO - Initializing Telegram notifier...
2026-01-18 10:00:11,070 - telegram_notifier - INFO - Message sent successfully...
2026-01-18 10:00:11,071 - __main__ - INFO - ✅ Telegram connection test successful!
2026-01-18 10:00:11,072 - scheduler - INFO - Scheduled reminder at 09:00
2026-01-18 10:00:11,072 - scheduler - INFO - Scheduled reminder at 12:00
2026-01-18 10:00:11,076 - __main__ - INFO - 🚀 Water Reminder Bot is now running!
```

### ตัวอย่าง Log แจ้งเตือน

```
2026-01-18 12:00:01,123 - scheduler - INFO - Sending reminder scheduled for 12:00
2026-01-18 12:00:02,456 - telegram_notifier - INFO - Message sent successfully: Lunch time reminder! Stay hydrated! 🌊
2026-01-18 12:00:02,457 - scheduler - INFO - Reminder sent successfully at 12:00
```

---

## 🔧 การแก้ไขปัญหา

### ปัญหา: ไม่ได้รับข้อความแจ้งเตือน

#### 1. ตรวจสอบว่า Service ทำงานอยู่หรือไม่

```bash
launchctl list | grep waterreminder
```

ถ้าไม่เห็น service → เริ่ม service ใหม่:

```bash
launchctl start com.waterreminder.daemon
```

#### 2. ตรวจสอบ Log

```bash
tail -n 50 /Users/coraline/Documents/alert_water/logs/daemon.log
```

#### 3. ตรวจสอบการเชื่อมต่อ Internet

บอทต้องมีการเชื่อมต่ออินเทอร์เน็ตเพื่อส่งข้อความ

#### 4. ตรวจสอบ Telegram Bot Token และ Chat ID

```bash
cat /Users/coraline/Documents/alert_water/.env
```

ตรวจสอบว่า:
- `TELEGRAM_BOT_TOKEN` ถูกต้อง
- `TELEGRAM_CHAT_ID` ถูกต้อง
- ไม่มี space ส่วนเกิน

### ปัญหา: Bot Token ไม่ถูกต้อง

**ข้อความ error ใน log:**
```
Invalid bot token. Please check TELEGRAM_BOT_TOKEN.
```

**วิธีแก้:**

1. ตรวจสอบ token กับ @BotFather บน Telegram
2. แก้ไขไฟล์ `.env`:

```bash
nano /Users/coraline/Documents/alert_water/.env
```

3. Restart service:

```bash
launchctl restart com.waterreminder.daemon
```

### ปัญหา: Chat ID ไม่ถูกต้อง

**ข้อความ error ใน log:**
```
Invalid chat ID or message format. Please check TELEGRAM_CHAT_ID.
```

**วิธีแก้:**

1. ส่งข้อความถึง bot ของคุณบน Telegram
2. ไปที่: `https://api.telegram.org/bot<YOUR_TOKEN>/getUpdates`
3. ดู `chat.id` ใน JSON response
4. อัปเดต `TELEGRAM_CHAT_ID` ในไฟล์ `.env`
5. Restart service

### ปัญหา: Service ไม่ start หลัง reboot

**วิธีแก้:**

1. ตรวจสอบว่า plist file อยู่ในตำแหน่งที่ถูกต้อง:

```bash
ls -la ~/Library/LaunchAgents/com.waterreminder.daemon.plist
```

2. โหลด service ใหม่:

```bash
launchctl unload ~/Library/LaunchAgents/com.waterreminder.daemon.plist
launchctl load ~/Library/LaunchAgents/com.waterreminder.daemon.plist
launchctl start com.waterreminder.daemon
```

### ปัญหา: เวลาแจ้งเตือนไม่ตรง

**สาเหตุ:** ระบบใช้เวลาจากนาฬิกาของเครื่อง

**วิธีแก้:**

1. ตรวจสอบเวลาของเครื่อง:

```bash
date
```

2. ถ้าไม่ถูกต้อง → ไปที่ `System Settings > General > Date & Time`
3. เปิด `Set time and date automatically`

### ปัญหา: บอทส่งข้อความซ้ำ

**สาเหตุ:** อาจมีหลาย instance ของบอทรันอยู่

**วิธีแก้:**

```bash
# หาทุก process ของ Python
ps aux | grep "python main.py"

# ฆ่าทุก process (ใช้ PID จากคำสั่งก่อนหน้า)
kill -9 <PID1> <PID2> ...

# เริ่ม service ใหม่
launchctl restart com.waterreminder.daemon
```

---

## ⚙️ การปรับแต่ง

### เปลี่ยนเวลาแจ้งเตือน

แก้ไขไฟล์ `/Users/coraline/Documents/alert_water/scheduler.py`:

```python
REMINDER_MESSAGES = {
    "09:00": "Good morning! Time to drink water! 💧",
    "12:00": "Lunch time reminder! Stay hydrated! 🌊",
    "15:00": "Afternoon hydration break! 💦",
    "18:00": "Evening water reminder! 🚰",
    "21:00": "Last call for water today! Good night! 🌙",
}
```

**ตัวอย่างการเพิ่มเวลา:**

```python
REMINDER_MESSAGES = {
    "08:00": "Early morning hydration! 💧",
    "10:00": "Mid-morning water break! 💦",
    "14:00": "Afternoon drink! 🌊",
    "16:00": "Pre-evening hydration! 💧",
    "20:00": "Evening water time! 🚰",
}
```

หลังแก้ไข → restart service:

```bash
launchctl restart com.waterreminder.daemon
```

### เปลี่ยนข้อความแจ้งเตือน

แก้ไขไฟล์ `/Users/coraline/Documents/alert_water/scheduler.py` ตามวิธีข้างบน

**ตัวอย่างข้อความภาษาไทย:**

```python
REMINDER_MESSAGES = {
    "09:00": "อรุณสวัสดิ์! ถึงเวลาดื่มน้ำแล้ว 💧",
    "12:00": "เที่ยงแล้ว อย่าลืมดื่มน้ำนะ 🌊",
    "15:00": "บ่ายแล้ว ลุกดื่มน้ำหน่อย 💦",
    "18:00": "เย็นนี้ดื่มน้ำให้พอ 🚰",
    "21:00": "คืนนี้อย่าลืมดื่มน้ำก่อนนอน 🌙",
}
```

### เพิ่ม/ลด จำนวนครั้งแจ้งเตือน

**เพิ่มครั้งแจ้งเตือน:**

```python
REMINDER_MESSAGES = {
    "09:00": "...",
    "12:00": "...",
    "15:00": "...",
    "18:00": "...",
    "21:00": "...",
    "23:00": "Before bed hydration! 🌙",  # เพิ่มใหม่
}
```

**ลดครั้งแจ้งเตือน:**

```python
REMINDER_MESSAGES = {
    "09:00": "...",
    "15:00": "...",
    "21:00": "...",
}
```

### เปลี่ยน Telegram Bot

ถ้าต้องการใช้ bot อื่น:

1. สร้าง bot ใหม่กับ @BotFather
2. ได้ token ใหม่
3. แก้ไขไฟล์ `.env`:

```bash
TELEGRAM_BOT_TOKEN=<NEW_TOKEN_HERE>
TELEGRAM_CHAT_ID=<YOUR_CHAT_ID>
```

4. Restart service:

```bash
launchctl restart com.waterreminder.daemon
```

---

## ❓ คำถามที่พบบ่อย (FAQ)

### Q1: บอทใช้งานได้ฟรีหรือไม่?

**A:** ใช่ ฟรีทั้งหมด! Telegram Bot API ไม่มีค่าใช้จ่าย

### Q2: ต้องเปิดคอมพิวเตอร์ตลอดเวลาหรือไม่?

**A:**
- ต้องเปิดคอมพิวเตอร์เพื่อรับข้อความแจ้งเตือน
- ถ้าปิดคอม → บอทไม่ส่งข้อความ
- เมื่อเปิดคอมใหม่ → บอท start อัตโนมัติและแจ้งเตือนตามเวลาถัดไป

### Q3: ถ้าฉันอยู่ที่อื่น จะได้รับข้อความหรือไม่?

**A:** ได้! ถ้า:
- คอมพิวเตอร์ที่บ้านเปิดอยู่
- เชื่อมต่ออินเทอร์เน็ต
- บอททำงานอยู่
- คุณเปิด Telegram บนมือถือ

คุณจะได้รับข้อความแจ้งเตือนทุกที่!

### Q4: สามารถเปลี่ยนเวลาแจ้งเตือนได้หรือไม่?

**A:** ได้! ดูวิธีในส่วน [การปรับแต่ง](#การปรับแต่ง)

### Q5: สามารถเพิ่มคนรับข้อความได้หรือไม่?

**A:**
- ในรุ่นปัจจุบัน **ไม่รองรับ** (ส่งให้ chat ID เดียวเท่านั้น)
- หากต้องการเพิ่มฟีเจอร์นี้ ติดต่อผู้พัฒนาเพื่อปรับปรุง

### Q6: บอทใช้ Internet มากไปหรือไม่?

**A:** ไม่เลย!
- ส่งข้อความ 5 ครั้ง/วัน
- ใช้ bandwidth น้อยมาก (เพียง few KB ต่อวัน)

### Q7: ถ้าต้องการหยุดใช้ชั่วคราว ทำอย่างไร?

**A:**

```bash
# หยุด service
launchctl stop com.waterreminder.daemon

# เริ่มใหม่เมื่อต้องการ
launchctl start com.waterreminder.daemon
```

### Q8: ต้องการถอนการติดตั้งทั้งหมด ทำอย่างไร?

**A:**

```bash
# 1. หยุดและถอน service
launchctl stop com.waterreminder.daemon
launchctl unload ~/Library/LaunchAgents/com.waterreminder.daemon.plist

# 2. ลบไฟล์ plist
rm ~/Library/LaunchAgents/com.waterreminder.daemon.plist

# 3. ลบโฟลเดอร์โปรเจกต์ (ถ้าต้องการ)
rm -rf /Users/coraline/Documents/alert_water
```

### Q9: Log ใหญ่เกินไป ทำอย่างไร?

**A:** ล้าง log ได้:

```bash
# หยุด service ก่อน
launchctl stop com.waterreminder.daemon

# ล้าง log
> /Users/coraline/Documents/alert_water/logs/daemon.log
> /Users/coraline/Documents/alert_water/logs/daemon.error.log

# เริ่ม service ใหม่
launchctl start com.waterreminder.daemon
```

### Q10: ต้องการ backup ข้อมูล ทำอย่างไร?

**A:**

```bash
# Backup ทั้งโฟลเดอร์โปรเจกต์
cp -r /Users/coraline/Documents/alert_water ~/backup/alert_water_$(date +%Y%m%d)

# Backup เฉพาะไฟล์สำคัญ
cp /Users/coraline/Documents/alert_water/.env ~/backup/.env.backup
cp ~/Library/LaunchAgents/com.waterreminder.daemon.plist ~/backup/
```

---

## 📞 ติดต่อผู้พัฒนา

หากพบปัญหาหรือมีข้อสงสัยเพิ่มเติม:

1. ตรวจสอบ log ก่อนเสมอ
2. อ่านคู่มือนี้ซ้ำ
3. ติดต่อผู้พัฒนาพร้อมรายละเอียด:
   - ปัญหาที่พบ
   - Log ที่เกี่ยวข้อง
   - Screenshot ถ้ามี

---

## 📝 อัปเดตล่าสุด

- **วันที่**: 18 มกราคม 2026
- **เวอร์ชัน**: 1.0
- **สถานะ**: พร้อมใช้งาน ✅

---

## 🎉 ขอให้มีสุขภาพดีด้วยการดื่มน้ำให้พอเหมาะ!

"การดื่มน้ำให้เพียงพอ เป็นรากฐานของสุขภาพที่ดี"

💧🌊💦🚰🌙
